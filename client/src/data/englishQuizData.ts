import { Quiz } from '../types';

/**
 * English quizzes and questions are stored in the PostgreSQL database
 * and fetched dynamically via GET /api/quizzes and GET /api/quizzes/:quizId.
 */
export const englishQuizzes: Quiz[] = [];

